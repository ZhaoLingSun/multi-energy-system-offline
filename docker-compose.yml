version: "3.9"
services:
  meos-offline:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - /home/ace/gurobi.lic:/app/gurobi.lic:ro
    environment:
      - GRB_LICENSE_FILE=/app/gurobi.lic
    command: ["python", "run_all.py", "--mip-gap", "1e-3", "--threads", "64"]
